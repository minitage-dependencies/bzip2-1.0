################################################################################
# MINITAGE LIBRARY PROJECT BUILDOUT
################################################################################
# the project part is maybe the only one you will need to modify for a new
# project
[project]
eggs-directory=${buildout:directory}/../../eggs/cache
develop-eggs-directory=${buildout:directory}/develop-eggs
directory=${buildout:directory}/../../
dependencies=${buildout:directory}/../../dependencies
parts =
    part
#########################################
# Buildout settings
# Modify at your own risks
#########################################
[buildout]
python = python2.4
versions = versions
executable = python2.4
parts = ${project:parts}
hooks-directory = ${buildout:directory}/hooks
parts-directory = ${parts:directory}
eggs-directory = ${project:eggs-directory}
develop-eggs-directory = ${project:develop-eggs-directory}
develop =

# Set our 2.4 python
[python2.4]
executable=${project:dependencies}/python-2.4/part/bin/python
name=python2.4 

[versions]
bopen.recipe.libinc = 0.2.1
hexagonit.recipe.cmmi = 1.1.1
hexagonit.recipe.download = 1.2.0
setuptools = 0.6c8
zc.recipe.egg = 1.0.0

[parts]
directory=${buildout:directory}/

[part]
recipe = minitage.recipe.cmmi
url = http://${mirrors:gentoo}/gentoo/distfiles/bzip2-1.0.4.tar.gz
md5sum = fc310b254f6ba5fbb5da018f04533688
noconfigure = true
nomake=true
noinstall = true
post-make-hook = ${buildout:hooks-directory}/cmmi-hooks.py:bz2
additionnal-make-targets =
    CFLAGS="$(bash -c "if [[ $(uname) == 'Darwin' ]];then echo '-mmacosx-version-min=10.5.0';fi") -fpic -Wall -Winline -O3  -I. -L.  -Wl,-rpath  -Wl,'${parts:directory}/part/lib' -Wl,-rpath -Wl,'/lib'"

[mirrors]
minitage = distfiles.minitage.org
